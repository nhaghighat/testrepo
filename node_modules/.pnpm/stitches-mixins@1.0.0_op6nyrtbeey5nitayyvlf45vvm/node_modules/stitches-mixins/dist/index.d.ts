import type { CSS } from "@stitches/core";
import { TDefaultMixin } from "./private";
export interface IMixins {
    [key: string]: CSS;
}
export declare const mixins: <T extends IMixins = {}>(customMixins?: T) => (key: TDefaultMixin | keyof T | (TDefaultMixin | keyof T)[]) => {};

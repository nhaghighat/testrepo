import { ValidRange } from '../../constants';
export declare const isValidSemverRange: (value: string) => value is ValidRange;
export declare const isSemver: (version: string) => boolean;
export declare const isLooseSemver: (version: string) => boolean;
